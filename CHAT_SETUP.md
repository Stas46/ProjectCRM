# Инструкция по настройке AI Чата

## 1. Создание таблиц в Supabase

Откройте Supabase SQL Editor и выполните файл `chat-schema.sql`:

1. Перейдите в Supabase Dashboard
2. Откройте SQL Editor
3. Скопируйте содержимое файла `chat-schema.sql`
4. Вставьте и выполните

Это создаст:
- Таблицу `chat_messages` - для хранения сообщений
- Таблицу `chat_usage_stats` - для статистики использования
- RLS политики для безопасности
- Триггеры для автоматического подсчета статистики

## 2. Настройка OpenAI API Key

1. Получите API ключ на https://platform.openai.com/api-keys
2. Добавьте в файл `.env.local`:

```env
OPENAI_API_KEY=sk-your-key-here
```

## 3. Перезапуск приложения

После добавления ключа перезапустите dev сервер:

```bash
npm run dev
```

## 4. Использование

1. Откройте главную страницу
2. Нажмите на карточку "AI Чат" 🤖
3. Выберите модель из выпадающего списка
4. Начните диалог!

## Доступные модели

- **GPT-4o** - самая новая модель (дорогая)
  - Промпт: $2.5/1M токенов
  - Ответ: $10/1M токенов

- **GPT-4o Mini** - быстрая и дешевая ⭐
  - Промпт: $0.15/1M токенов
  - Ответ: $0.6/1M токенов

- **GPT-4 Turbo** - мощная модель
  - Промпт: $10/1M токенов
  - Ответ: $30/1M токенов

- **GPT-3.5 Turbo** - бюджетный вариант
  - Промпт: $0.5/1M токенов
  - Ответ: $1.5/1M токенов

## Функции

✅ История сообщений (хранится в БД)
✅ Выбор модели
✅ Подсчет токенов и стоимости
✅ Статистика использования (сообщения, токены, стоимость за день)
✅ Очистка истории
✅ Автоматическое сохранение в Supabase
✅ RLS - каждый пользователь видит только свои сообщения

## Цены обновлены на 10.11.2024

Актуальные цены можно проверить на: https://openai.com/api/pricing/
