# Cloudflare Worker для OpenAI API Proxy

Этот воркер проксирует запросы к OpenAI API, обходя блокировку в России.

## 🚀 Быстрое развертывание

### Шаг 1: Создайте аккаунт Cloudflare
1. Перейдите на https://dash.cloudflare.com/sign-up
2. Зарегистрируйтесь (бесплатно)
3. Подтвердите email

### Шаг 2: Создайте Worker
1. Откройте https://dash.cloudflare.com/
2. В боковом меню выберите **Workers & Pages**
3. Нажмите **Create Worker**
4. Cloudflare предложит имя (можете изменить на `openai-proxy`)
5. Нажмите **Deploy**

### Шаг 3: Вставьте код
1. После деплоя нажмите **Edit Code**
2. Удалите весь код в редакторе
3. Скопируйте содержимое файла `openai-proxy.js` из этой папки
4. Вставьте в редактор
5. Нажмите **Save and Deploy**

### Шаг 4: Получите URL
После деплоя вы увидите URL вашего воркера, например:
```
https://openai-proxy.your-subdomain.workers.dev
```

### Шаг 5: Настройте приложение

#### На локальной машине:
Откройте `.env.local` и добавьте:
```bash
OPENAI_BASE_URL=https://openai-proxy.your-subdomain.workers.dev/v1
```

#### На сервере:
```bash
ssh root@your-server
cd /var/www/alu.stella-spb.ru
nano .env.local
```

Добавьте строку:
```bash
OPENAI_BASE_URL=https://openai-proxy.your-subdomain.workers.dev/v1
```

Сохраните (Ctrl+O, Enter, Ctrl+X) и перезапустите:
```bash
pm2 restart crm-glazing
```

## ✅ Проверка работы

Протестируйте воркер:
```bash
curl -X POST https://openai-proxy.your-subdomain.workers.dev/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_OPENAI_API_KEY" \
  -d '{
    "model": "gpt-4o-mini",
    "messages": [{"role": "user", "content": "Say hello"}],
    "max_tokens": 10
  }'
```

Должен вернуться ответ от OpenAI.

## 📊 Лимиты Cloudflare Workers (Free Plan)

- **100,000 запросов/день** ✅
- **10ms CPU time** на запрос
- **Unlimited** для streaming

Этого более чем достаточно для вашего CRM!

## 🔒 Безопасность

- Воркер не логирует API ключи
- Работает как прозрачный прокси
- Ваш OPENAI_API_KEY остаётся в .env.local (не передавайте его в воркер)

## 🐛 Troubleshooting

### Ошибка "Worker threw exception"
- Проверьте, что скопировали весь код
- Убедитесь, что нет синтаксических ошибок

### 403 Forbidden
- Проверьте, что OPENAI_API_KEY корректный
- Убедитесь, что URL воркера правильный в .env.local

### Медленные ответы
- Cloudflare Workers работают на Edge, должны быть быстрыми
- Проверьте логи в Cloudflare Dashboard → Workers → Logs

## 💡 Альтернативы

Если не хотите использовать Cloudflare:

1. **OpenRouter**: https://openrouter.ai
2. **Together AI**: https://together.ai
3. **Groq**: https://groq.com (очень быстрый)

Все совместимы с OpenAI SDK!
