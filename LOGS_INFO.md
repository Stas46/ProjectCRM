# Система логирования задач

## Где найти логи

Все логи из `/tasks` записываются в файл:
```
glazing-crm/tasks-debug.log
```

## Как это работает

1. **Frontend** (`src/app/tasks/TasksPage.tsx`):
   - Функция `logToFile()` собирает логи в буфер
   - Каждые 2 секунды отправляет накопленные логи на сервер через POST `/api/log`

2. **Backend** (`src/app/api/log/route.ts`):
   - Принимает логи в формате `{ content: string }`
   - Добавляет их в конец файла `tasks-debug.log`

3. **Формат логов**:
   ```
   2024-12-10T12:34:56.789Z - 📥 loadTasks - Starting task load...
   2024-12-10T12:34:56.890Z - ✅ loadTasks - Loaded 5 tasks from DB
   2024-12-10T12:35:01.234Z - ☑️ toggleDone - Task: Test task (ID: abc-123)
   ```

## Основные события в логах

- `📥 loadTasks` - загрузка задач из БД
- `☑️ toggleDone` - переключение статуса выполнено/не выполнено
- `🗑️ deleteTask` - удаление задачи
- `↩️ undoDelete` - отмена удаления
- `✏️ editTitle` - редактирование названия
- `📦 moveToQuadrant` - перемещение между квадрантами матрицы
- `➕ createTask` - создание новой задачи
- `⭐ Task is IMPORTANT` - задача помечена как важная (priority=1)
- `⚡ Task is URGENT` - задача помечена как срочная (status=in_progress или due_date<=3 дня)
- `📊 Grouping tasks` - группировка по квадрантам Эйзенхауэра

## Как просмотреть логи

### Вариант 1: PowerShell
```powershell
Get-Content tasks-debug.log -Tail 50  # последние 50 строк
Get-Content tasks-debug.log -Wait     # в реальном времени (как tail -f)
```

### Вариант 2: VS Code
Просто откройте файл `tasks-debug.log` в редакторе

### Вариант 3: Фильтрация
```powershell
# Только ошибки
Get-Content tasks-debug.log | Select-String "❌"

# Только создание задач
Get-Content tasks-debug.log | Select-String "createTask"

# Логи за последний час (примерно)
Get-Content tasks-debug.log -Tail 1000
```

## Очистка логов

Если файл стал слишком большой:
```powershell
Clear-Content tasks-debug.log
```

Или просто удалите файл - он создастся заново при первой записи.

## Отладка проблем

При сообщении о баге:
1. Воспроизведите проблему
2. Подождите 2-3 секунды (чтобы логи успели записаться)
3. Скопируйте последние 30-50 строк из `tasks-debug.log`
4. Приложите к описанию проблемы

Это позволит мне (агенту) точно понять последовательность событий и состояние данных.
