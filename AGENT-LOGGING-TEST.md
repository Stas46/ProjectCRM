# Инструкция по тестированию системы логирования AI-агентов

## ✅ Что уже готово

1. **База данных**
   - SQL-схема: `create-agent-logs-table.sql`
   - Таблица: `agent_logs` с полями для детального логирования
   - RLS-политики: пользователи видят свои логи, админы — все

2. **Backend**
   - `src/lib/agent-logger.ts` — утилита для логирования
   - `src/lib/data-agent.ts` — полностью инструментирован
   - `src/app/api/agent-logs/route.ts` — API для получения логов

3. **Frontend**
   - `src/app/logs/page.tsx` — страница просмотра логов
   - Фильтры: agent_type, action_type, status, session_id
   - Раскрывающиеся карточки с детальной информацией

4. **Dev-сервер**
   - ✅ Запущен на http://localhost:3000

---

## 🔧 Шаг 1: Создать таблицу agent_logs в Supabase

### В Supabase Dashboard:

1. Открыть **SQL Editor**
2. Скопировать содержимое файла `create-agent-logs-table.sql`
3. Выполнить SQL-запрос
4. Проверить в **Table Editor**, что таблица `agent_logs` создана

### Проверка через SQL:
```sql
SELECT COUNT(*) FROM agent_logs;
-- Должно вернуть 0 (таблица пуста)
```

---

## 🧪 Шаг 2: Тестирование логирования

### 1. Открыть чат
- Перейти на http://localhost:3000/chat
- Войти под своим аккаунтом
- Переключиться на **"Личный помощник"**

### 2. Задать тестовый вопрос
Примеры:
- "какие задачи на сегодня?"
- "покажи мои проекты"
- "список последних счетов"

### 3. Открыть страницу логов
- Перейти на http://localhost:3000/logs
- Должны появиться записи с типами:
  - `analyze_intent` (Data Agent)
  - `fetch_tasks` / `fetch_projects` / `fetch_invoices` (CRM Tools)

### 4. Проверить детали лога
Кликнуть на любой лог → раскроется детальная информация:

**analyze_intent:**
- 📥 **Входные данные**: текст вопроса пользователя
- 📤 **Выходные данные**: JSON с распознанным намерением
- 🤖 **Модель**: deepseek-chat
- 🎯 **Токены**: количество использованных токенов
- ⏱️ **Время выполнения**: миллисекунды

**fetch_tasks:**
- 📥 **Входные данные**: фильтры (date_range, priority, status)
- 📤 **Выходные данные**: массив задач
- 📊 **Строк**: сколько задач найдено
- ⏱️ **Время выполнения**: миллисекунды

---

## 🔍 Шаг 3: Диагностика проблемы "0 задач"

### Если личный помощник возвращает "На сегодня задач не найдено":

1. **Открыть логи** → найти последнюю сессию
2. **Проверить analyze_intent:**
   - Правильно ли распознал намерение?
   - Какой `action_type` установлен? (должен быть `get_tasks`)
   - Какие фильтры в `filters`?

3. **Проверить fetch_tasks:**
   - Сколько строк вернулось? (`rows_affected`)
   - Есть ли ошибка? (`error_message`)
   - Если 0 строк — проверить фильтры:
     - `date_range`: правильный ли диапазон дат?
     - `assigned_to`: совпадает ли с `user_id`?

### Возможные причины:

#### A. Задач действительно нет
```sql
-- Проверить в Supabase:
SELECT * FROM tasks WHERE assigned_to = 'ВАШ_USER_ID';
```

#### B. Некорректный date_range
- Лог покажет: `{ date_range: "2024-01-15 - 2024-01-15" }`
- Проверить, что сегодня входит в этот диапазон

#### C. RLS блокирует доступ
```sql
-- Проверить политики:
SELECT * FROM tasks WHERE assigned_to = auth.uid();
```

#### D. Неправильный SQL-запрос
- В логе будет поле `sql_query` (когда добавим в crm-data-tools.ts)
- Можно скопировать и выполнить вручную в SQL Editor

---

## 📊 Шаг 4: Фильтрация логов

### По типу агента:
- **Data Agent** — анализ намерений (DeepSeek)
- **CRM Tools** — запросы к базе данных
- **Assistant Agent** — формирование ответов

### По статусу:
- ✅ **Успех** — операция выполнена
- ❌ **Ошибка** — произошла ошибка (смотреть `error_message`)
- ⚠️ **Предупреждение** — есть проблемы, но не критичные

### По сессии:
- Скопировать `session_id` из одного лога
- Вставить в фильтр "ID сессии"
- Увидеть всю цепочку действий для одного вопроса

---

## 🐛 Известные проблемы и решения

### 1. Таблица agent_logs не создается
```sql
-- Проверить, что RLS включен:
ALTER TABLE agent_logs ENABLE ROW LEVEL SECURITY;

-- Проверить политики:
SELECT * FROM pg_policies WHERE tablename = 'agent_logs';
```

### 2. API /api/agent-logs возвращает 401
- Проверить, что пользователь авторизован
- Проверить токен в заголовке Authorization

### 3. Логи не отображаются на странице
- Открыть DevTools → Console
- Проверить, нет ли ошибок JavaScript
- Проверить Network → /api/agent-logs (статус 200?)

### 4. Время выполнения = null
- Проверить, что функция использует `startAgentLog()`
- Проверить, что вызывается `finish()` callback

---

## 🚀 Шаг 5: После успешного локального теста

1. **Закоммитить изменения:**
```bash
git add .
git commit -m "Добавлена система логирования AI-агентов"
git push
```

2. **В production Supabase:**
- Выполнить `create-agent-logs-table.sql` в SQL Editor
- Проверить создание таблицы

3. **Проверить деплой:**
- GitHub Actions автоматически развернет код
- Открыть https://alu.stella-spb.ru/logs
- Проверить работу логирования в production

---

## 📝 Что показывают логи

### Для разработчика:
- **Производительность**: сколько времени занимает каждая операция
- **Потребление токенов**: сколько стоят AI-запросы
- **Ошибки**: где и почему падает код
- **SQL-запросы**: какие данные запрашиваются

### Для отладки:
- **Трассировка**: полная цепочка действий от вопроса до ответа
- **Входные данные**: что передал пользователь
- **Выходные данные**: что вернул AI/база данных
- **Промежуточные результаты**: распознанное намерение, фильтры, количество строк

---

## 🎯 Следующие улучшения

1. **Логирование SQL-запросов** в `crm-data-tools.ts`:
   - Добавить поле `sql_query` в логи
   - Видеть точные WHERE-условия

2. **Метрики производительности**:
   - Медленные запросы (>1000ms)
   - Большое потребление токенов (>5000)

3. **Автоматические алерты**:
   - Email при ошибках
   - Telegram-уведомления

4. **Дашборд статистики**:
   - График использования по дням
   - Топ запросов пользователей
   - Средняя стоимость запроса

---

## ✨ Ожидаемый результат

После выполнения всех шагов:

✅ Страница /logs работает  
✅ Логи записываются в базу данных  
✅ Можно фильтровать по агентам, статусам, сессиям  
✅ Детальная информация доступна в раскрывающихся карточках  
✅ Видна причина проблемы "0 задач на сегодня"  

🎉 **Система полностью готова к отладке и мониторингу AI-агентов!**
