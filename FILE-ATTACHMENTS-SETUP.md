# Настройка файловых вложений для чата

## 🎨 Что поддерживается

### ✅ Изображения (Vision) - РАБОТАЕТ
- **Форматы**: PNG, JPG, WEBP, GIF
- **Модели**: GPT-4o 🎨, GPT-4o Mini 🎨
- **Возможности**:
  - Анализ содержимого изображений
  - OCR (чтение текста с фото)
  - Анализ графиков, таблиц, диаграмм
  - Описание визуальных элементов

### ✅ Документы (PDF, DOCX, TXT) - РАБОТАЕТ! NEW!
- **Форматы**: PDF, DOCX, TXT, MD, CSV, JSON
- **Технология**: OpenAI Responses API + Files API
- **Возможности**:
  - Чтение и анализ PDF документов
  - Извлечение данных из Word файлов
  - Анализ контрактов, договоров, счетов
  - Поиск юридических рисков
  - Суммаризация длинных документов

## Подробности в файлах
- `VISION-SUPPORT.md` - полная документация
- `DOCUMENT-ANALYSIS.md` - техническая реализация анализа документов
- `QUICK-START-VISION.md` - быстрый старт

## Шаги для активации функции

### 1. Выполнить SQL для создания Storage bucket

1. Откройте Supabase Dashboard → SQL Editor
2. Скопируйте содержимое файла **create-storage-bucket.sql**
3. Выполните SQL
4. Проверьте что бакет создан: Storage → chat-files должен появиться

### 2. Выполнить SQL для обновления схемы базы данных

1. Откройте Supabase Dashboard → SQL Editor
2. Скопируйте содержимое файла **chat-attachments-schema.sql**
3. Выполните SQL
4. Проверьте результат в конце вывода - должны показаться таблицы и поля

### 3. Проверить что всё работает

1. Запустите приложение: `npm run dev`
2. Откройте http://localhost:3000/chat
3. Нажмите на кнопку скрепки (📎) рядом с полем ввода
4. Выберите файл (изображение или документ)
5. Файл должен появиться в превью над полем ввода
6. Отправьте сообщение
7. Файл должен отобразиться в сообщении

## Что добавлено

### Интерфейс
- ✅ Кнопка скрепки для выбора файлов
- ✅ Превью выбранных файлов с возможностью удаления
- ✅ Отображение файлов в сообщениях (картинки как превью, документы как ссылки)
- ✅ Иконки для разных типов файлов

### Функционал
- ✅ Загрузка файлов в Supabase Storage (bucket: chat-files)
- ✅ Сохранение метаданных файлов в базу данных (attachments JSONB)
- ✅ Валидация: максимум 10MB на файл
- ✅ Поддержка множественной загрузки (можно выбрать несколько файлов)
- ✅ Отображение размера файлов

### Безопасность
- ✅ RLS политики - пользователи видят только свои файлы
- ✅ Файлы хранятся в папках по user_id
- ✅ Публичный доступ к файлам через защищенные URL

## Структура хранения

```
Supabase Storage:
chat-files/
  └── {user_id}/
      └── {timestamp}_{filename}

Database:
chat_messages.attachments = [
  {
    file_name: "document.pdf",
    file_type: "application/pdf",
    file_size: 123456,
    file_url: "https://..."
  }
]
```

## Поддерживаемые файлы

- Изображения (PNG, JPG, GIF, WebP и т.д.)
- Документы (PDF, DOC, DOCX, TXT и т.д.)
- Максимальный размер: 10MB

## Если что-то не работает

1. Проверьте что бакет создан в Storage
2. Проверьте что поле attachments добавлено в chat_messages
3. Проверьте консоль браузера на ошибки
4. Проверьте логи в терминале где запущен npm run dev
