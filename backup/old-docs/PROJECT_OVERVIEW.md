# 🏗️ CRM для управления проектами остекления

## 📖 Описание проекта

Это веб-приложение для управления проектами остекления с автоматическим распознаванием счетов и накладных.

### Основные возможности:
1. ✅ Управление проектами остекления
2. ✅ Учет клиентов (физлица и юрлица)
3. ✅ Управление поставщиками
4. ✅ Автоматическое распознавание счетов (OCR)
5. ✅ Управление задачами и бригадами
6. ✅ Учет сотрудников
7. ✅ Календарь смен
8. ✅ Система сообщений по проектам

---

## 🛠️ Технологический стек

### Frontend:
- **Next.js 15.5.3** - React фреймворк
- **TypeScript** - типизация
- **Tailwind CSS** - стили
- **Radix UI** - компоненты
- **Lucide Icons** - иконки

### Backend:
- **Supabase** - база данных (PostgreSQL) + аутентификация
- **Row Level Security (RLS)** - политики безопасности
- **REST API** - через Supabase

### Интеграции:
- **Google Cloud Vision API** - OCR распознавание документов
- **Yandex Vision API** - альтернативное OCR
- **Python** - скрипты для парсинга счетов

---

## 📁 Структура проекта

```
glazing-crm/
├── src/
│   ├── app/                    # Next.js App Router страницы
│   │   ├── page.tsx           # Главная страница (дашборд)
│   │   ├── projects/          # Управление проектами
│   │   │   ├── page.tsx       # Список проектов
│   │   │   └── new/           # Создание проекта
│   │   ├── tasks/             # Задачи
│   │   ├── invoices/          # Счета и накладные
│   │   ├── suppliers/         # Поставщики
│   │   ├── calendar/          # Календарь смен
│   │   ├── profile/           # Профиль пользователя
│   │   └── api/               # API routes
│   │       └── invoices/      # API для работы со счетами
│   ├── components/            # React компоненты
│   │   ├── app-layout.tsx    # Общий layout приложения
│   │   └── ui/               # UI компоненты (кнопки, формы и т.д.)
│   └── lib/                   # Утилиты и библиотеки
│       ├── supabase.ts       # Клиент Supabase
│       └── seed-database.ts  # Заполнение тестовыми данными
│
├── public/                    # Статические файлы
│   ├── test-center/          # Тестовый центр для OCR
│   └── *.html                # Утилиты и тесты
│
├── python-scripts/            # Python скрипты
│   └── ultimate_invoice_parser.py  # Парсер счетов
│
├── database-schema/           # Документация БД
│   ├── 01-clients.md
│   ├── 02-projects.md
│   ├── 03-suppliers.md
│   ├── 04-invoices.md
│   └── ...
│
├── supabase/                  # Конфигурация Supabase
│
├── DATABASE_SCHEMA.md         # 📖 ГЛАВНЫЙ СПРАВОЧНИК ПО БД
├── .env.local                 # Переменные окружения
├── package.json              # Зависимости npm
└── next.config.ts            # Конфигурация Next.js
```

---

## 🔑 Переменные окружения (.env.local)

```bash
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://fpnugtlchxigwpqwiczc.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon_key>
SUPABASE_SERVICE_ROLE_KEY=<service_role_key>

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=<your_secret>

# Google Cloud Vision (OCR)
GOOGLE_CLOUD_PROJECT_ID=<project_id>
GOOGLE_CLOUD_VISION_API_KEY=<api_key>

# Yandex Cloud Vision (OCR)
YANDEX_CLOUD_FOLDER_ID=<folder_id>
YANDEX_CLOUD_API_KEY=<api_key>
```

---

## 🚀 Запуск проекта

### Установка зависимостей:
```bash
npm install
```

### Запуск в режиме разработки:
```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

---

## 📊 База данных

**Подробная документация**: см. `DATABASE_SCHEMA.md`

### Основные таблицы:
1. **projects** - проекты остекления
2. **clients** - клиенты
3. **invoices** - счета и накладные
4. **suppliers** - поставщики
5. **tasks** - задачи
6. **employees** - сотрудники
7. **crews** - бригады
8. **shifts** - смены

### Подключение к БД:
```typescript
import { supabase } from '@/lib/supabase';

// Получение данных
const { data, error } = await supabase
  .from('projects')
  .select('*');
```

---

## 🤖 Система распознавания счетов (OCR)

### Как работает:
1. Пользователь загружает PDF/изображение счета
2. Файл отправляется на сервер
3. Сервер вызывает Google Cloud Vision API или Yandex Vision API
4. OCR распознает текст и структурированные данные
5. Python-парсер обрабатывает результат
6. Данные автоматически заполняются в форме счета

### Файлы OCR системы:
- `src/app/api/invoices/route.ts` - API endpoint
- `python-scripts/ultimate_invoice_parser.py` - парсер
- `public/test-center/` - тестовый интерфейс

### Тестирование OCR:
1. Запустите проект: `npm run dev`
2. Откройте: http://localhost:3000/test-center
3. Загрузите тестовый счет
4. Проверьте результаты распознавания

---

## 🔐 Безопасность (RLS)

В Supabase настроены Row Level Security политики:

```sql
-- Пример политики
CREATE POLICY "Allow all operations for anon" 
ON public.projects
USING (true)
WITH CHECK (true);
```

**Важно**: Для production необходимо настроить более строгие политики!

---

## 📝 Типичные задачи

### Создание нового проекта:
```typescript
// 1. Создаем клиента
const { data: client } = await supabase
  .from('clients')
  .insert({
    type: 'individual',
    first_name: 'Иван',
    last_name: 'Иванов',
    phone: '+7 900 123-45-67'
  })
  .select()
  .single();

// 2. Создаем проект
const { data: project } = await supabase
  .from('projects')
  .insert({
    name: 'Остекление балкона',    // НЕ title!
    client_id: client.id,            // НЕ client!
    address: 'г. Москва, ул. Ленина, д. 1',
    status: 'planning',
    start_date: '2025-11-10',
    end_date: '2025-12-10',
    budget: 150000
  })
  .select();
```

### Загрузка счета с OCR:
```typescript
const formData = new FormData();
formData.append('file', file);
formData.append('project_id', projectId);

const response = await fetch('/api/invoices', {
  method: 'POST',
  body: formData
});

const result = await response.json();
```

---

## 🐛 Отладка

### Проверка подключения к БД:
```bash
# Откройте Supabase SQL Editor
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public';
```

### Просмотр логов:
- В браузере: DevTools → Console
- На сервере: см. терминал с `npm run dev`

---

## 📚 Дополнительные ресурсы

- [Next.js Documentation](https://nextjs.org/docs)
- [Supabase Documentation](https://supabase.com/docs)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [TypeScript Handbook](https://www.typescriptlang.org/docs)

---

## ⚠️ Важные замечания

1. **Всегда сверяйтесь с `DATABASE_SCHEMA.md`** перед созданием форм!
2. Используйте `name` вместо `title` для проектов
3. Используйте `client_id` (UUID), а не `client` (string)
4. Даты: `start_date`, `end_date` (не `due_date`)
5. Перед продакшеном настройте строгие RLS политики
6. Регулярно обновляйте документацию при изменениях в БД

---

## 🆘 Частые проблемы

### "Could not find the 'client' column"
- ✅ Используйте `client_id` вместо `client`
- ✅ Сверьтесь с `DATABASE_SCHEMA.md`

### "permission denied for schema public"
- ✅ Проверьте RLS политики в Supabase
- ✅ Используйте правильный API ключ

### OCR не распознает текст
- ✅ Проверьте API ключи Google/Yandex
- ✅ Убедитесь что файл читаемый (хорошее качество)
- ✅ Проверьте логи в `/test-center`

---

Последнее обновление: 8 ноября 2025 г.
