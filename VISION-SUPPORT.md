# Поддержка файлов в AI чате

## ✅ Что работает

### Анализ изображений (Vision)
- **Модели с поддержкой**: GPT-4o 🎨, GPT-4o Mini 🎨
- **Форматы**: PNG, JPEG, WEBP, GIF
- **Максимальный размер**: 10MB (настроено в UI), 50MB (лимит API)
- **Что может AI**:
  - Описывать содержимое изображения
  - Читать текст с фотографий (OCR)
  - Анализировать диаграммы, графики, таблицы
  - Отвечать на вопросы по изображению
  - Сравнивать несколько изображений

### ✅ Анализ документов (NEW!)
- **Модели с поддержкой**: GPT-4o 🎨, GPT-4o Mini 🎨
- **Форматы**: **PDF только!** (OpenAI Responses API)
- **Максимальный размер**: 50MB на файл, 50MB общий размер запроса
- **Технология**: OpenAI Responses API + Files API
- **Что может AI**:
  - Читать и анализировать PDF документы
  - Извлекать информацию из PDF
  - Анализировать контракты, договоры, счета
  - Отвечать на вопросы по содержимому
  - Суммировать длинные документы
  - Находить юридические риски

⚠️ **DOCX, TXT, CSV не поддерживаются** - только PDF через Responses API

### Примеры использования

**Пример 1: Анализ фотографии счета**
```
Пользователь: [прикладывает фото счета] Сколько здесь итоговая сумма?
AI: На фото счета видна итоговая сумма 1,250.00 рублей
```

**Пример 2: Чтение визитки**
```
Пользователь: [фото визитки] Какой номер телефона?
AI: На визитке указан номер: +7 (495) 123-45-67
```

**Пример 3: Анализ графика**
```
Пользователь: [график продаж] Какой тренд видишь?
AI: График показывает стабильный рост продаж с марта по июнь, затем небольшое снижение в июле
```

**Пример 4: Анализ PDF договора (NEW!)**
```
Пользователь: [contract.pdf] Проверь договор на риски исполнителя
AI: В разделе 5 отсутствует пункт о сроках расторжения.
    В пункте 7.3 возможен риск для исполнителя - штрафные санкции без встречных обязательств.
    Рекомендую добавить пункт о форс-мажорных обстоятельствах.
```

**Пример 5: Извлечение данных из DOCX**
```
Пользователь: [report.docx] Извлеки все email адреса из документа
AI: Найдены следующие email адреса:
    - support@company.com
    - sales@company.com
    - admin@company.com
```

## ⚠️ Ограничения

### Размеры файлов
- Изображения: до 50MB на файл
- PDF документы: до 50MB на файл
- Общий размер всех файлов в запросе: до 50MB

### Что НЕ поддерживается
- ❌ DOCX файлы (Responses API принимает только PDF)
- ❌ Excel файлы (XLSX) 
- ❌ TXT, MD, CSV напрямую (только PDF)
- ❌ Медицинские снимки (КТ, МРТ) - не предназначено для медицинской диагностики
- ❌ Мелкий текст на изображениях (может быть нечитаем)
- ❌ Отсканированные документы плохого качества

### Особенности PDF
- ✅ PDF с текстом - читается идеально
- ✅ PDF со сканами - OpenAI извлекает текст и изображения страниц
- ⚠️ Токены считаются за текст + изображение каждой страницы (дорого для больших PDF)
- ⚠️ **Только PDF поддерживается** для анализа документов через Responses API

## 🔧 Техническая реализация

### Как работает Vision (изображения)

1. **Загрузка изображения**
   - Файл загружается в Supabase Storage
   - Генерируется публичный URL
   - URL сохраняется в базе данных

2. **Отправка в OpenAI Chat Completions API**
   ```json
   {
     "role": "user",
     "content": [
       { "type": "text", "text": "Что на изображении?" },
       {
         "type": "image_url",
         "image_url": {
           "url": "https://supabase.../image.jpg",
           "detail": "high"
         }
       }
     ]
   }
   ```

### Как работает анализ документов (NEW!)

1. **Загрузка документа**
   - Файл загружается в Supabase Storage
   - Генерируется публичный URL
   - URL сохраняется в базе данных

2. **Загрузка в OpenAI Files API**
   ```js
   const file = await openai.files.create({
     file: fileBlob,
     purpose: "user_data"
   });
   // Получаем file_id: file-abc123xyz
   ```

3. **Отправка в OpenAI Responses API**
   ```json
   {
     "model": "gpt-4o",
     "input": [{
       "role": "user",
       "content": [
         { "type": "input_text", "text": "Проанализируй документ" },
         { "type": "input_file", "file_id": "file-abc123xyz" }
       ]
     }]
   }
   ```

4. **Обработка OpenAI**
   - Для PDF: извлекает текст + создаёт изображения страниц
   - Для DOCX: читает содержимое
   - AI анализирует и отвечает

### Автоматический выбор API

Код автоматически выбирает правильный API:
- **Только изображения** → Chat Completions API
- **Есть документы (PDF/DOCX/TXT)** → Responses API + Files API
- **Смешанный контент** → Responses API (поддерживает всё)

### Стоимость Vision

Изображения считаются в токенах по специальной формуле:

**GPT-4o**: 85 базовых токенов + 170 токенов за каждый квадрат 512px
**GPT-4o-mini**: 2833 токена на низком качестве, 5667 на высоком

**Пример**: Изображение 1024x1024 в режиме `high` = ~765 токенов для GPT-4o

## 📋 Roadmap

### ✅ Реализовано

1. **Анализ изображений через Vision** ✅
   - Chat Completions API с image_url
   - Поддержка GPT-4o, GPT-4o-mini
   - Режим `high` для детального анализа

2. **Анализ документов через Responses API** ✅ NEW!
   - Автоматическая загрузка в Files API
   - Поддержка PDF, DOCX, TXT, MD, CSV, JSON
   - Умный выбор API (Chat Completions vs Responses)

### Планируется добавить

1. **Кэширование загруженных файлов** ✨
   - Хранить file_id в БД чтобы не загружать повторно
   - Удаление файлов из OpenAI через 30 дней

2. **Drag & Drop для файлов** 🎯
   - Перетаскивание файлов в окно чата
   - Вставка изображений из буфера обмена (Ctrl+V)

3. **Предпросмотр перед отправкой** 👁️
   - Миниатюры изображений
   - Превью содержимого документов
   - Индикатор загрузки файлов

4. **Excel поддержка** 📊
   - Конвертация XLSX → CSV
   - Анализ таблиц и данных

## 🎯 Рекомендации

### Для лучших результатов

1. **Используйте GPT-4o** вместо Mini для важных изображений
2. **Четкие фото** - избегайте размытых или темных снимков
3. **Ориентация** - переворачивайте изображения правильно перед загрузкой
4. **Размер текста** - текст должен быть читаемым (не слишком мелкий)
5. **Множественные изображения** - можно прикрепить несколько за раз

### Примеры хороших запросов

✅ "Прочитай номер счета на этом фото"
✅ "Какие товары указаны в таблице?"
✅ "Сравни эти два графика и найди различия"
✅ "Извлеки все контактные данные с визитки"

### Примеры плохих запросов

❌ "Что это?" (слишком общий)
❌ Размытое фото с мелким текстом
❌ Перевернутое изображение
❌ Очень большое изображение (>10MB)

## 🚀 Как использовать

### Изображения
1. Выберите модель с Vision (GPT-4o или GPT-4o Mini)
2. Нажмите кнопку скрепки 📎
3. Выберите изображение (PNG, JPG, WEBP, GIF)
4. Напишите вопрос или оставьте пустым для общего описания
5. Нажмите "Отправить"
6. AI проанализирует изображение и ответит

### Документы (PDF, DOCX, TXT)
1. Выберите модель GPT-4o или GPT-4o Mini
2. Нажмите кнопку скрепки 📎
3. Выберите документ (PDF, DOCX, TXT, MD, CSV, JSON)
4. Напишите вопрос по документу
5. Нажмите "Отправить"
6. Система:
   - Загрузит файл в OpenAI Files API
   - Автоматически переключится на Responses API
   - AI прочитает и проанализирует документ
   - Вернёт ответ с анализом

### Смешанные запросы
Можно прикладывать одновременно:
- Изображения + текст
- Документы + текст
- Изображения + документы + текст

## 📊 Статистика

После отправки вы увидите:
- Количество использованных токенов
- Стоимость запроса в долларах
- Изображение в истории сообщений
