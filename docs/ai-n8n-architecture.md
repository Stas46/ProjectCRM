# AI + n8n Integration Architecture

## Проблема текущей реализации

**Сейчас:**
```
Telegram → Data Agent → createTask() → БД
                              ↓
                         n8n webhook → Telegram (уведомление)
```

**Минусы:**
- n8n только для уведомлений
- AI и n8n не связаны
- Логика дублируется (и в коде, и в n8n)

## Правильная архитектура

### Цель: n8n как центральный оркестратор действий AI

```
Telegram Bot
    ↓
Data Agent (DeepSeek) - анализ намерения
    ↓
ВМЕСТО прямого INSERT в БД → n8n webhook
    ↓
n8n Workflow:
  1. Создаёт задачу в Supabase
  2. Создаёт событие в календаре (опционально)
  3. Отправляет email (опционально)
  4. Отправляет Telegram уведомление
  5. Логирует в БД
  6. Возвращает подтверждение
    ↓
AI получает ответ от n8n
    ↓
Форматирует ответ пользователю
```

## Преимущества

1. **Единая точка для всех действий** - вся логика в n8n UI
2. **Легко добавлять новые интеграции** - без правок кода
3. **Retry и error handling** - встроено в n8n
4. **Мониторинг** - все executions видны в n8n
5. **A/B тесты** - можно условно маршрутизировать

## Реализация

### Шаг 1: Модифицировать Data Agent

Вместо:
```typescript
// src/lib/data-agent.ts
const { data, error } = await createTask(userId, taskData);
```

Делать:
```typescript
// Отправляем в n8n
const n8nResponse = await fetch('https://n8n.alu.stella-spb.ru/webhook/ai-action', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    action: 'create_task',
    user_id: userId,
    data: taskData,
    telegram_id: telegramId
  })
});

const result = await n8nResponse.json();
// result = { success: true, task_id: "uuid", message: "Задача создана" }
```

### Шаг 2: n8n Workflow "AI Actions"

```
Webhook: /webhook/ai-action
  ↓
Switch по action:
  ├─ create_task
  │   ├─ Supabase Insert
  │   ├─ Send Telegram
  │   └─ Return success
  │
  ├─ update_task
  │   ├─ Supabase Update
  │   ├─ Send Telegram
  │   └─ Return success
  │
  └─ get_tasks
      ├─ Supabase Query
      └─ Return data
```

### Шаг 3: AI форматирует ответ

Data Agent получает от n8n:
```json
{
  "success": true,
  "task_id": "uuid-123",
  "message": "Задача создана",
  "notifications_sent": ["telegram", "email"]
}
```

И отдаёт пользователю:
```
✅ Создал задачу "Купить крышки"
📱 Отправил уведомление в Telegram
📧 Отправил на email
```

## Пример полного workflow

### Создание задачи через AI

**1. Пользователь в Telegram:**
```
"создай важную задачу позвонить поставщику завтра в 10:00"
```

**2. Data Agent (DeepSeek) анализирует:**
```json
{
  "action": "create_task",
  "task": {
    "title": "Позвонить поставщику",
    "priority": 1,
    "due_date": "2025-11-13T10:00:00Z",
    "description": "Важный звонок"
  }
}
```

**3. Data Agent → n8n webhook:**
```bash
POST https://n8n.alu.stella-spb.ru/webhook/ai-action
{
  "action": "create_task",
  "user_id": "uuid",
  "telegram_id": 123456789,
  "data": {
    "title": "Позвонить поставщику",
    "priority": 1,
    "due_date": "2025-11-13T10:00:00Z"
  }
}
```

**4. n8n выполняет:**
- ✅ INSERT в Supabase tasks
- ✅ Создать событие в Google Calendar (опционально)
- ✅ Отправить Telegram: "🆕 Новая задача: Позвонить поставщику"
- ✅ Настроить напоминание за 1 час (через n8n Schedule)
- ✅ Вернуть: `{"success": true, "task_id": "..."}`

**5. Data Agent → GPT-4 форматирование:**
```
✅ Создал задачу "Позвонить поставщику"
⏰ Дедлайн: завтра в 10:00
📱 Отправил уведомление
🔔 Напомню за час
```

**6. Ответ пользователю в Telegram**

## Дополнительные возможности

### AI может запрашивать подтверждение через n8n

```
Пользователь: "удали все старые задачи"
  ↓
AI: "Найдено 15 задач старше месяца. Удалить?"
  ↓
n8n отправляет в Telegram inline кнопки: [Да] [Нет]
  ↓
Пользователь нажимает [Да]
  ↓
n8n получает callback → удаляет задачи → уведомляет AI
  ↓
AI: "✅ Удалил 15 задач"
```

### Multi-step workflows

```
AI: "создай счёт на 50000 руб для компании ABC"
  ↓
n8n workflow:
  1. Создать счёт в БД
  2. Сгенерировать PDF (Code Interpreter)
  3. Загрузить в Supabase Storage
  4. Отправить на email директору
  5. Отправить в Telegram уведомление
  6. Если сумма > 50к → отправить SMS директору
  7. Создать запись в 1C (через API)
  8. Вернуть AI: все выполнено
  ↓
AI форматирует красивый ответ пользователю
```

## Сравнение подходов

### Текущий (код + n8n для уведомлений)
```
✅ Просто
✅ Быстро работает
❌ Дублирование логики
❌ Сложно добавлять новые интеграции
❌ n8n недоиспользован
```

### Предлагаемый (n8n как оркестратор)
```
✅ Вся логика в одном месте (n8n UI)
✅ Легко добавлять интеграции
✅ Retry, мониторинг, логирование
✅ AI + n8n = мощная связка
❌ Нужно переделать Data Agent
❌ Чуть медленнее (дополнительный HTTP вызов)
```

## Гибридный подход (рекомендую!)

**Простые операции** (get_tasks, get_projects):
```
AI → Supabase напрямую → Ответ
```

**Сложные операции** (create_task, update_task, create_invoice):
```
AI → n8n webhook → Supabase + уведомления + интеграции → Ответ
```

**Критичные операции** (delete_all, массовые изменения):
```
AI → n8n → Approval через Telegram → Action → Ответ
```

## Что делать дальше?

1. ✅ Оставить текущую реализацию (уведомления работают)
2. 🔄 Постепенно переводить действия на n8n:
   - Сначала: create_task через n8n
   - Потом: update_task
   - Затем: create_invoice
   - И так далее
3. 🎯 Добавлять сложные workflow по мере необходимости

## Пример миграции

### До (прямой вызов):
```typescript
// data-agent.ts
const { data, error } = await createTask(userId, {
  title: "Купить крышки",
  priority: 1
});
```

### После (через n8n):
```typescript
// data-agent.ts  
const result = await executeN8nAction('create_task', {
  user_id: userId,
  telegram_id: telegramId,
  task: {
    title: "Купить крышки",
    priority: 1
  }
});
// n8n сам создаст задачу + отправит уведомление + логирует
```

Хотите реализовать этот подход? Начнём с простого workflow для create_task через n8n!
