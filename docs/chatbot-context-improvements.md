# Улучшения контекстного понимания Telegram-бота

**Дата:** 13 ноября 2025  
**Цель:** Улучшить понимание контекста разговора и запросов пользователя

---

## 🎯 Решённые проблемы

### 1. ❌ "Какой цвет конструкций в объекте ТСЖ окна"
**Было:** Бот не понимал, что речь о проекте "Окно ТСЖ" из контекста  
**Стало:** Бот использует частичный поиск по названию проекта и извлекает данные из CRM

### 2. ❌ "Пришли последние задачи по южному шоссе объекту"
**Было:** Бот не находил задачи по частичному совпадению названия  
**Стало:** Поиск работает для нескольких проектов с похожим названием ("Южное шоссе козырьки", "Южное шоссе витражи")

### 3. ❌ "добавь эти файлы к проекту"
**Было:** Бот давал инструкции по Git вместо работы с CRM  
**Стало:** Бот понимает контекст предыдущего разговора о проекте

---

## 🚀 Внесённые изменения

### 1. Улучшен системный промпт (`data-agent.ts`)

Добавлены инструкции:

```typescript
КОНТЕКСТ ДИАЛОГА - КРИТИЧЕСКИ ВАЖНО:
1. ЗАПОМИНАЙ последние упомянутые проекты, счета, задачи
2. Если пользователь спрашивает про "этот/тот/такой/его/последний" - ищи в ПРЕДЫДУЩИХ сообщениях
3. "добавь файлы к проекту" - если обсуждали проект, НЕ давай инструкции по git!
4. "какой цвет" / "детали" / "задачи" - если был проект в контексте, ищи по ЭТОМУ проекту
5. "южное шоссе" - может быть 2 проекта - ищи ОБА

УМНЫЙ ПОИСК ПО НАЗВАНИЯМ:
- Используй ЧАСТИЧНОЕ совпадение: "южное" → найти все с "Южное шоссе"
- "тсж" / "ТСЖ" / "окно тсж" → "Окно ТСЖ"
- "школа" → найти проект "Школа" 
- Игнорируй регистр и лишние пробелы
```

### 2. Добавлено поле `context_project` в интерфейс

```typescript
export interface DataAgentRequest {
  // ... остальные поля
  context_project?: string; // Название проекта из контекста диалога
}
```

### 3. Частичный поиск в `crm-data-tools.ts`

**Добавлен фильтр `project_name`:**
```typescript
export interface DataQueryFilters {
  // ... остальные фильтры
  project_name?: string; // Частичный поиск по названию проекта
}
```

**В `getUserProjects()`:**
```typescript
if (filters?.project_name) {
  query = query.ilike('project_name', `%${filters.project_name}%`);
}
```

**В `getUserTasks()` - поиск задач по названию проекта:**
```typescript
// Если передан project_name, сначала найдем проекты
if (filters?.project_name) {
  const projectsResult = await getUserProjects(userId, { 
    project_name: filters.project_name 
  });
  if (projectsResult.data && projectsResult.data.length > 0) {
    projectIds = projectsResult.data.map(p => p.id);
    query = query.in('project_id', projectIds);
  }
}
```

---

## 📊 Примеры улучшенного понимания

### До улучшений ❌
```
Пользователь: "Какой цвет конструкций в объекте ТСЖ окна"
Бот: "🏠 Обычно окна в ТСЖ окрашивают в нейтральные цвета: белый..."
```

### После улучшений ✅
```
Пользователь: "Какой цвет конструкций в объекте ТСЖ окна"
Бот: [Ищет проект "Окно ТСЖ" в БД]
     "Проект 'Окно ТСЖ': [детали из базы]"
```

---

### До улучшений ❌
```
Пользователь: "Пришли последние задачи по южному шоссе"
Бот: "Задачи по объекту «Южное шоссе» не найдены 🤷‍♂️"
```

### После улучшений ✅
```
Пользователь: "Пришли последние задачи по южному шоссе"
Бот: [Находит проекты: "Южное шоссе козырьки", "Южное шоссе витражи"]
     [Показывает задачи по обоим проектам]
```

---

### До улучшений ❌
```
Контекст: обсуждали проект "Окно ТСЖ"
Пользователь: "добавь эти файлы к проекту"
Бот: "git add имя_файла1 имя_файла2..."
```

### После улучшений ✅
```
Контекст: обсуждали проект "Окно ТСЖ"
Пользователь: "добавь эти файлы к проекту"
Бот: [Понимает контекст, добавляет файлы в проект "Окно ТСЖ" в CRM]
```

---

## 🔧 Технические детали

### Файлы изменены:
1. `src/lib/data-agent.ts` - улучшен промпт (строки 33-108)
2. `src/lib/crm-data-tools.ts` - добавлен частичный поиск:
   - `DataQueryFilters.project_name` (строка 56)
   - `getUserProjects()` с `ilike()` (строки 125-138)
   - `getUserTasks()` с каскадным поиском (строки 68-122)

### Использованные технологии:
- **PostgreSQL `ILIKE`** - регистронезависимый поиск с `%` wildcards
- **DeepSeek Chat** - анализ намерений с расширенным промптом
- **Контекстное поле** `context_project` - для отслеживания проектов в диалоге

---

## 📝 Следующие шаги

Рекомендуется дополнительно реализовать:

1. **Отправка файлов счетов** - когда пользователь просит "Пришли счет №..."
2. **Интеграция платежей** - "Отправь в оплату счет №..."
3. **Хранение контекста диалога** - сохранять последние упомянутые сущности в сессии
4. **Улучшенное форматирование** - красивые карточки проектов вместо текста

---

## ✅ Готово к тестированию

Изменения применены и готовы к тестированию в Telegram-боте.

**Протестировать:**
```
1. "Какие проекты по южному шоссе?"
2. "Задачи по школе"
3. "Детали проекта ТСЖ"
4. [После обсуждения проекта] "Какие счета по этому проекту?"
```
