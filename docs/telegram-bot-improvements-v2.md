# Улучшения Telegram-бота v2.0 - Расширенная аналитика

**Дата:** 13 ноября 2025  
**Версия:** 2.0  
**Статус:** ✅ Готово к тестированию

---

## 🎯 Новые возможности

### 1. 📊 Анализ бюджета проекта
Бот теперь знает и может показать:
- **Бюджет проекта** и общую сумму расходов
- **Остаток бюджета** и процент выполнения
- **Расходы по категориям** (профиль, фурнитура, стекло и т.д.)
- **Расходы по поставщикам** (топ-5 поставщиков)
- **Статистику по счетам** (оплаченные/неоплаченные)

**Примеры запросов:**
```
"Бюджет проекта Школа"
"Сколько осталось денег по ТСЖ окна?"
"Показать расходы по Южному шоссе"
```

### 2. 🏷️ Фильтрация счетов по категориям
Теперь можно искать счета по типу товаров:

**Поддерживаемые категории:**
- Профиль / профиля
- Фурнитура
- Стекло / стеклопакет
- Уплотнитель / резина
- Крепёж / саморезы

**Примеры запросов:**
```
"Сколько потратили на профиль?"
"Счета по фурнитуре"
"Покажи все закупки стекла"
```

### 3. 🏢 Фильтрация по поставщикам
Поиск счетов от конкретного поставщика с детализацией:

**Примеры запросов:**
```
"Счета от Проведал"
"Что покупали у Проведал?"
"Сколько заплатили Татпроф?"
```

**Ответ включает:**
- Список всех счетов от поставщика
- Общую сумму
- Категории товаров
- Описание товаров (если есть поле `items`)

### 4. 📝 Заметки проекта
Бот читает и показывает заметки из карточки проекта:

**Примеры запросов:**
```
"Детали проекта Школа"
"Какие заметки по ТСЖ?"
"Информация о проекте Южное шоссе"
```

### 5. 📅 Саммари по задачам
Интеллектуальный анализ задач проекта:

**Что показывает:**
- Группировка по статусам (завершено/в работе/ожидает)
- Группировка по приоритетам (важные/средние/низкие)
- ⚠️ Просроченные задачи
- 🔥 Задачи со сроком сегодня
- 📅 Задачи на ближайшие 3 дня
- 🔥 Топ важных задач в работе

**Примеры запросов:**
```
"Саммари по задачам"
"Итог по задачам проекта Школа"
"Статус задач"
```

### 6. 🕐 Знание текущей даты
Бот всегда знает сегодняшнюю дату и учитывает её при анализе сроков:

**Системный промпт обновляется:**
```
ТЕКУЩАЯ ДАТА: среда, 13 ноября 2025 г.
```

### 7. 💾 Контекст диалога (2-3 запроса)
Бот запоминает последние упомянутые сущности:

**Хранит в памяти:**
- Последние 3 проекта
- Последние 3 счета  
- Последние 3 задачи
- Время жизни контекста: 30 минут

**Примеры уточняющих вопросов:**
```
Пользователь: "Какие проекты есть?"
Бот: [Показывает список с "Школа", "ТСЖ окна"]

Пользователь: "Покажи счета по второму"
Бот: [Понимает, что речь о "ТСЖ окна", показывает счета]

Пользователь: "Сколько потратили на профиль?"
Бот: [Фильтрует счета по ТСЖ окна И категории "профиль"]
```

---

## 🔧 Технические изменения

### Обновлённые типы данных

**`Project` (crm-data-tools.ts):**
```typescript
interface Project {
  id: string;
  project_name?: string;
  project_number?: string;
  budget?: number;        // 💰 Бюджет проекта
  notes?: string;         // 📝 Заметки проекта
  // ... остальные поля
}
```

**`Invoice` (crm-data-tools.ts):**
```typescript
interface Invoice {
  id: string;
  invoice_number: string;
  category?: string;      // 🏷️ Категория (профиль, фурнитура и т.д.)
  items?: string;         // 📦 Описание товаров
  suppliers?: {           // 🏢 Связь с таблицей suppliers
    name: string;
    inn?: string;
  };
  // ... остальные поля
}
```

**`DataQueryFilters`:**
```typescript
interface DataQueryFilters {
  category?: string;       // Фильтр по категории товаров
  supplier_name?: string;  // Фильтр по имени поставщика
  project_name?: string;   // Частичный поиск по проекту
  // ... остальные фильтры
}
```

### Новые функции

**1. `getProjectBudgetStats(projectId)`**
```typescript
// Возвращает полную статистику по бюджету:
{
  project: Project;
  budget: number;
  spent: number;
  remaining: number;
  invoices_by_category: Array<{ category, total, count }>;
  invoices_by_supplier: Array<{ supplier, total, count }>;
  total_invoices: number;
  paid_invoices: number;
  unpaid_invoices: number;
}
```

**2. `createTasksSummary(tasks)`**
```typescript
// Создаёт текстовое саммари с анализом:
- Группировка по статусам и приоритетам
- Просроченные задачи
- Задачи на сегодня и ближайшие дни
- Важные задачи в работе
```

**3. Контекст диалога (in-memory)**
```typescript
interface DialogContext {
  userId: string;
  lastProjects: Array<{ id, name }>;
  lastInvoices: Array<{ id, number }>;
  lastTasks: Array<{ id, title }>;
  lastUpdated: Date;
}

// TTL: 30 минут
```

### Обновлённый системный промпт

**Добавлено в DATA_AGENT_SYSTEM_PROMPT:**

```
ТЕКУЩАЯ ДАТА: ${new Date().toLocaleDateString('ru-RU')}

Доступные действия:
6. **get_budget** - показать бюджет проекта, расходы по категориям, остаток

КАТЕГОРИИ ТОВАРОВ (для фильтрации счетов):
- "профиль" / "профиля" - алюминиевый профиль
- "фурнитура" - ручки, петли, замки
- "стекло" / "стеклопакет"
- "уплотнитель" / "резина"
- "крепёж" / "саморезы"

КОНТЕКСТ ДИАЛОГА:
1. ЗАПОМИНАЙ последние 2-3 упомянутых проекта, счета, задачи
7. "саммари по задачам" / "итог" / "статус" → need_summary: true
```

**Новые примеры:**
```json
"бюджет проекта школа" → {
  "action": "get_budget",
  "filters": {"project_name": "школа"},
  "reasoning": "Статистика по бюджету"
}

"сколько потратили на профиль" → {
  "action": "get_invoices",
  "filters": {"category": "профиль"},
  "reasoning": "Счета по категории профиль"
}

"саммари по задачам" → {
  "action": "get_tasks",
  "need_summary": true,
  "reasoning": "Создать саммари"
}
```

---

## 📊 Примеры использования

### Сценарий 1: Анализ бюджета проекта

```
Пользователь: "Бюджет по проекту Школа"

Бот: 
💰 БЮДЖЕТ ПРОЕКТА "Школа"

Бюджет: 500 000 ₽
Потрачено: 387 420 ₽
Остаток: 112 580 ₽
Прогресс: 77.5%

📋 Счета: 15 шт.
✅ Оплачено: 12 шт.
❌ Не оплачено: 3 шт.

📊 ПО КАТЕГОРИЯМ:
• Профиль: 8 шт. на 245 000 ₽
• Фурнитура: 4 шт. на 89 420 ₽
• Стекло: 3 шт. на 53 000 ₽

🏢 ПО ПОСТАВЩИКАМ:
• Проведал: 8 шт. на 245 000 ₽
• Татпроф: 4 шт. на 89 420 ₽
• СтеклоПром: 3 шт. на 53 000 ₽

📝 Заметки:
Проект для школы №5, требуется усиленный профиль
```

### Сценарий 2: Фильтрация по категориям

```
Пользователь: "Сколько потратили на профиль в проекте ТСЖ?"

Бот:
📋 Найдено 5 счетов на профиль

1. Счет №3131 - 26 537 ₽ (21.10.2024)
   Проведал • Профиль АТ-50
   
2. Счет №3254 - 9 850 ₽ (01.11.2024)
   Проведал • Профиль АТ-40
   
3. Счет №6 - 4 891 ₽ (23.10.2024)
   ТатПроф • Профиль усиленный

📊 ПО КАТЕГОРИЯМ:
Профиль: 5 шт. на 45 278 ₽
```

### Сценарий 3: Работа с контекстом

```
Пользователь: "Какие у нас проекты?"
Бот: [Показывает 10 проектов, включая "Школа", "ТСЖ окна", "Южное шоссе"]

Пользователь: "Детали по школе"
Бот: [Показывает информацию о проекте "Школа" из контекста]

Пользователь: "Счета по этому проекту"
Бот: [Показывает счета проекта "Школа", используя сохранённый контекст]

Пользователь: "Сколько потратили на фурнитуру?"
Бот: [Фильтрует счета проекта "Школа" по категории "фурнитура"]
```

### Сценарий 4: Саммари задач

```
Пользователь: "Саммари по задачам проекта Школа"

Бот:
📊 САММАРИ ПО ЗАДАЧАМ

Всего задач: 12

📈 По статусам:
✅ Завершено: 8
🔄 В работе: 3
⏳ Ожидает: 1

🎯 По приоритетам:
🔴 Важные: 4
🟡 Средние: 6
🟢 Низкие: 2

⚠️ ПРОСРОЧЕНО: 1 задача!
   - Согласовать смету с заказчиком

🔥 СРОК СЕГОДНЯ: 2 задачи
   - Заказать профиль
   - Подписать договор

📅 СРОК В БЛИЖАЙШИЕ 3 ДНЯ: 1 задача
   - Доставка фурнитуры (15.11.2025)

🔥 ВАЖНЫЕ ЗАДАЧИ В РАБОТЕ:
   - Монтаж конструкций
   - Проверка качества
```

---

## ✅ Контрольный список для тестирования

### Бюджет проекта
- [ ] "Бюджет проекта Школа"
- [ ] "Сколько осталось денег по ТСЖ?"
- [ ] "Показать расходы по Южному шоссе"

### Категории товаров
- [ ] "Сколько потратили на профиль?"
- [ ] "Счета по фурнитуре"
- [ ] "Покажи закупки стекла для ТСЖ"

### Поставщики
- [ ] "Счета от Проведал"
- [ ] "Что покупали у Татпроф?"
- [ ] "Сколько заплатили поставщику X?"

### Заметки проектов
- [ ] "Детали проекта Школа"
- [ ] "Какие заметки по ТСЖ?"

### Саммари задач
- [ ] "Саммари по задачам"
- [ ] "Итог по задачам Школа"
- [ ] "Статус всех задач"

### Контекст диалога
- [ ] Последовательность: проекты → детали → счета
- [ ] Уточняющие вопросы ("по этому проекту", "сколько потратили")
- [ ] Переключение контекста между проектами

### Дата и сроки
- [ ] Просроченные задачи
- [ ] Задачи на сегодня
- [ ] Задачи на ближайшие дни

---

## 🚀 Развёртывание

### 1. Проверить переменные окружения
```bash
DEEPSEEK_API_KEY=sk-...
NEXT_PUBLIC_SUPABASE_URL=https://...
SUPABASE_SERVICE_ROLE_KEY=eyJ...
```

### 2. Обновить схему БД (если нужно)

**Добавить поля в таблицу `projects`:**
```sql
ALTER TABLE projects 
ADD COLUMN IF NOT EXISTS budget NUMERIC,
ADD COLUMN IF NOT EXISTS notes TEXT;
```

**Добавить поля в таблицу `invoices`:**
```sql
ALTER TABLE invoices 
ADD COLUMN IF NOT EXISTS category VARCHAR(100),
ADD COLUMN IF NOT EXISTS items TEXT;
```

### 3. Перезапустить сервер
```bash
npm run dev
```

---

## 📈 Метрики успеха

После внедрения отслеживать:
- Количество запросов по бюджету
- Использование фильтров по категориям
- Частоту запросов саммари
- Процент успешного распознавания контекста
- Время ответа бота (должно быть < 3 сек)

---

## 🎓 Обучение пользователей

Отправить в Telegram-канал команды:

```
🎉 НОВЫЕ ВОЗМОЖНОСТИ БОТА!

Теперь бот умеет ещё больше:

💰 Анализ бюджета:
   "Бюджет проекта Школа"
   "Сколько осталось?"

🏷️ Фильтры по категориям:
   "Сколько потратили на профиль?"
   "Счета по фурнитуре"

🏢 Поиск по поставщикам:
   "Что покупали у Проведал?"

📊 Саммари задач:
   "Итог по задачам"
   "Статус задач проекта X"

💬 Умные уточнения:
   Просто спрашивай "а по этому проекту?" 
   и бот поймёт контекст!
```

---

**Готово к использованию!** 🚀
